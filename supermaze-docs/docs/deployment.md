# Deployment

- Open `index.html` in any browser for local play.
- To host online:
  1. Push project to GitHub
  2. Deploy via GitHub Pages or any static host
  3. Ensure server features are accessible if using WebSocket leaderboard
